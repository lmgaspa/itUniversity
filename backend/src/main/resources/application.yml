spring:
  application:
    name: universitysystem

  datasource:
    url: postgres://u8svaenl54eb9p:pbe77fdafcfe14b49ee9d50e919
    username: uc68c5g21jl5gt
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

api:
  security:
    token:
      secret: my-secret-key

server:
  port: 8080
