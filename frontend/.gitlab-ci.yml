stages:
  - build
  - test
  - sonarqube
  - deploy

variables:
  BASE_DIR: "${CI_PROJECT_DIR}/frontend"
  SONAR_HOST_URL: "http://192.168.0.101:9000"
  SONAR_LOGIN: "${SONAR_TOKEN}"

build-job:
  stage: build
  tags:
    - build  # A tag deve corresponder à tag definida no runner
  script:
    - npm install
    - npm run build
